<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- ================= "BlackCat/JolieCat" Theme Palette ================= -->
    <!-- The unified global theme dictionary - every cross-window control Style used to
         live inline in App.xaml's own Application.Resources; it's split out here so the
         palette/Style set reads as one self-contained "brand" file, merged back into
         App.xaml via Application.Resources.MergedDictionaries. Window-specific Styles
         (a particular window's own one-off dropdown/tab/tool-button chrome) stay local to
         that window's own Resources instead - only Styles genuinely meant to apply
         anywhere in the app belong here. Warm charcoal/espresso + gold-sand palette,
         matching the 2D JolieCat.UI editor's own exact aesthetic (its browns/golds read
         warmer than the cooler neutral grays this file used before) - flat, solid-color
         brushes only, no gradients. -->
    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#FF1E1C1A" />
    <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#FF252321" />
    <SolidColorBrush x:Key="PanelAltBackgroundBrush" Color="#FF2D2A27" />
    <!-- A very subtle, warm near-black - the one place a hard, cool-white/light-gray
         default (WPF's own OS-theme border/splitter chrome) would otherwise clash
         hardest against this dark palette, so every divider line and GridSplitter stays
         nearly invisible against the panels it separates instead of standing out. -->
    <SolidColorBrush x:Key="BorderBrush" Color="#FF302E2C" />
    <SolidColorBrush x:Key="HoverBackgroundBrush" Color="#FF3A3733" />

    <SolidColorBrush x:Key="AccentBrush" Color="#FFC8A265" />
    <!-- The exact hex JolieCatLogo.ico's own background is - Tier 1's own custom title
         bar (MainWindow.xaml) uses this specifically so it matches the logo pixel-for-
         pixel, rather than the (close, but not the same) AccentBrush every other
         highlight/active-state color in this app already uses. -->
    <SolidColorBrush x:Key="TitleBarBackgroundBrush" Color="#FFE9C47D" />
    <SolidColorBrush x:Key="ActiveIndicatorBrush" Color="#FF5DA453" />

    <SolidColorBrush x:Key="TextPrimaryBrush" Color="#FFE0DCD5" />
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="#FFAFA89C" />

    <!-- Dark text for the one spot something sits on a gold fill (a selected
         Outliner row) instead of a dark panel - TextPrimaryBrush reads at a washed-
         out ~2:1 contrast there, same reasoning as JolieCat.UI's own OnAccentTextBrush. -->
    <SolidColorBrush x:Key="OnAccentTextBrush" Color="#FF1E1C1A" />

    <!-- Custom title bar's own Close button - a dedicated red hover, the one place this
         theme departs from the plain HoverBackgroundBrush every other hover state uses,
         matching the near-universal "Close goes red on hover" convention. -->
    <SolidColorBrush x:Key="CloseHoverBackgroundBrush" Color="#FFC0392B" />

    <!-- ================= Base Styles ================= -->
    <Style TargetType="{x:Type Window}">
        <Setter Property="Background" Value="{StaticResource WindowBackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="12" />
    </Style>

    <Style TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
    </Style>

    <Style x:Key="PanelHeaderStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Margin" Value="8,6,8,4" />
    </Style>

    <Style TargetType="{x:Type GridSplitter}">
        <Setter Property="Background" Value="{StaticResource BorderBrush}" />
        <Setter Property="ShowsPreview" Value="False" />
    </Style>

    <!-- Flat, modern, borderless card button - no BorderBrush/BorderThickness at all (the
         task's own "no borders, subtle highlight on hover" ask); depth comes only from the
         Background/HoverBackgroundBrush contrast against whatever panel it sits on. -->
    <Style TargetType="{x:Type Button}">
        <Setter Property="Background" Value="{StaticResource PanelAltBackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="10,4" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="Bg" Background="{TemplateBinding Background}" CornerRadius="4">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="{StaticResource HoverBackgroundBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- The gizmo mode toolbar's Translate/Rotate/Scale buttons - three RadioButtons
         (mutually exclusive by GroupName) restyled to look like a flat button strip,
         the checked one filled gold - matching JolieCat.UI's own "selected tool"
         convention (its Tools panel's active tile is the same gold fill). Keeps its own
         thin border (unlike the plain Button style above) so adjacent same-Background
         segments of the strip still read as separate buttons. -->
    <Style x:Key="GizmoModeButtonStyle" TargetType="{x:Type RadioButton}">
        <Setter Property="Background" Value="{StaticResource PanelAltBackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="12,4" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RadioButton}">
                    <Border x:Name="Bg" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="{StaticResource HoverBackgroundBrush}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="{StaticResource AccentBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Same "was entirely unstyled -> OS-default light background + this app's own
         dark-theme Foreground -> invisible white-on-white text" bug JolieCat.UI hit
         and fixed for TextBox/ComboBox, fixed here from the start instead. -->
    <Style TargetType="{x:Type TextBox}">
        <Setter Property="Background" Value="{StaticResource PanelAltBackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
        <Setter Property="CaretBrush" Value="{StaticResource TextPrimaryBrush}" />
        <Setter Property="SelectionBrush" Value="{StaticResource AccentBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="4,2" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3">
                        <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ComboBox was this app's one remaining unstyled control (every ComboBox
         throughout - Interaction Mode, Transform Space, every Modifier's own Axis/
         Operation picker, ...) previously rendered with WPF's own default OS-light
         chrome; a flat, dark, borderless-hover-highlight template like every other
         control above, following the exact same Toggle+Popup shape the Menu/MenuItem and
         Tier 2 header's own dropdown Styles already use. -->
    <Style TargetType="{x:Type ComboBox}">
        <Setter Property="Background" Value="{StaticResource PanelAltBackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="6,3" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBox}">
                    <Grid>
                        <ToggleButton x:Name="ToggleButtonPart" Focusable="False" ClickMode="Press"
                                      Background="{TemplateBinding Background}"
                                      BorderBrush="{TemplateBinding BorderBrush}"
                                      BorderThickness="{TemplateBinding BorderThickness}"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="{x:Type ToggleButton}">
                                    <Border x:Name="Bg" Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="1" Text="&#x25BE;" FontSize="9" Margin="0,0,8,0"
                                                       Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" />
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bg" Property="Background" Value="{StaticResource HoverBackgroundBrush}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>

                        <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="Left" VerticalAlignment="Center" />

                        <!-- Only reachable/required when IsEditable="True" (none of this
                             app's ComboBoxes are) - present anyway since ComboBox's own
                             base class looks up this exact template part by name. -->
                        <TextBox x:Name="PART_EditableTextBox" Visibility="Collapsed"
                                 Background="Transparent" BorderThickness="0" IsReadOnly="{TemplateBinding IsReadOnly}" />

                        <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True" Focusable="False" PopupAnimation="Fade">
                            <Border Background="{StaticResource PanelBackgroundBrush}"
                                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                                    CornerRadius="4" Padding="2"
                                    MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}">
                                <ScrollViewer MaxHeight="320" SnapsToDevicePixels="True">
                                    <StackPanel IsItemsHost="True" />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ComboBoxItem}">
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                    <Border x:Name="Bg" Background="Transparent" CornerRadius="3" Padding="{TemplateBinding Padding}">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="{StaticResource AccentBrush}" />
                            <Setter Property="Foreground" Value="{StaticResource OnAccentTextBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Same idea for TreeView/TreeViewItem (the Scene Outliner) - its default chrome
         is just as OS-themed/light as an unstyled ComboBox/TextBox, so it gets the
         same treatment before it ever ships unstyled. -->
    <Style TargetType="{x:Type TreeView}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="4" />
    </Style>

    <Style TargetType="{x:Type TreeViewItem}">
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
        <Setter Property="Padding" Value="4,3" />
        <Setter Property="IsExpanded" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TreeViewItem}">
                    <StackPanel>
                        <Border x:Name="Bg" CornerRadius="3">
                            <Grid Margin="{TemplateBinding Padding}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="14" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <!-- Expand/collapse chevron - hidden entirely for a
                                     leaf item with no children (HasItems=False). -->
                                <ToggleButton x:Name="Expander" Grid.Column="0" Focusable="False"
                                              ClickMode="Press" Width="14" Height="14"
                                              IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ToggleButton.Style>
                                        <Style TargetType="{x:Type ToggleButton}">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                                                        <Border Background="Transparent">
                                                            <TextBlock x:Name="Chevron" Text="&#x25BE;" FontSize="9"
                                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                                       HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="False">
                                                                <Setter TargetName="Chevron" Property="Text" Value="&#x25B8;" />
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ToggleButton.Style>
                                </ToggleButton>

                                <ContentPresenter Grid.Column="1" ContentSource="Header" VerticalAlignment="Center" />
                            </Grid>
                        </Border>

                        <ItemsPresenter x:Name="ItemsHost" Margin="14,0,0,0" />
                    </StackPanel>

                    <ControlTemplate.Triggers>
                        <Trigger Property="HasItems" Value="False">
                            <Setter TargetName="Expander" Property="Visibility" Value="Hidden" />
                        </Trigger>
                        <Trigger Property="IsExpanded" Value="False">
                            <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="{StaticResource HoverBackgroundBrush}" />
                        </Trigger>
                        <!-- Selected last, so it wins over hover when both are true. -->
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="{StaticResource AccentBrush}" />
                            <Setter Property="Foreground" Value="{StaticResource OnAccentTextBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Same idea again for Menu/MenuItem (the File menu) - the default chrome is
         a plain white popup with black text, the exact "OS-default light background
         + this app's own dark Foreground" combination that read as barely-visible
         (or actually invisible, for the top-level bar itself) every other unstyled
         control in this app already hit. Only TopLevelHeader (File itself) and
         SubmenuItem (Open/Save/...) are templated - this app's File menu is flat, no
         further nested submenus, so SubmenuHeader is never actually instantiated. -->
    <Style TargetType="{x:Type Menu}">
        <Setter Property="Background" Value="{StaticResource PanelAltBackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
    </Style>

    <Style TargetType="{x:Type Separator}">
        <Setter Property="Background" Value="{StaticResource BorderBrush}" />
        <Setter Property="Height" Value="1" />
        <Setter Property="Margin" Value="4,4" />
    </Style>

    <Style TargetType="{x:Type MenuItem}">
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Style.Triggers>
            <Trigger Property="Role" Value="TopLevelHeader">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type MenuItem}">
                            <Border x:Name="Bg" Background="{TemplateBinding Background}" Padding="10,6">
                                <Grid>
                                    <ContentPresenter ContentSource="Header" VerticalAlignment="Center" RecognizesAccessKey="True" />
                                    <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsSubmenuOpen}"
                                           AllowsTransparency="True" Focusable="False" PopupAnimation="Fade">
                                        <Border Background="{StaticResource PanelBackgroundBrush}"
                                                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                                                CornerRadius="4" Padding="2" MinWidth="180">
                                            <StackPanel IsItemsHost="True" />
                                        </Border>
                                    </Popup>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bg" Property="Background" Value="{StaticResource HoverBackgroundBrush}" />
                                </Trigger>
                                <Trigger Property="IsSubmenuOpen" Value="True">
                                    <Setter TargetName="Bg" Property="Background" Value="{StaticResource HoverBackgroundBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>

            <Trigger Property="Role" Value="SubmenuItem">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type MenuItem}">
                            <Border x:Name="Bg" Background="{TemplateBinding Background}" CornerRadius="3">
                                <Grid Margin="10,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0" ContentSource="Header" VerticalAlignment="Center" RecognizesAccessKey="True" />
                                    <TextBlock Grid.Column="1" Text="{TemplateBinding InputGestureText}" Margin="24,0,0,0"
                                               Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" />
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsHighlighted" Value="True">
                                    <Setter TargetName="Bg" Property="Background" Value="{StaticResource AccentBrush}" />
                                    <Setter Property="Foreground" Value="{StaticResource OnAccentTextBrush}" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.5" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Global default TabControl/TabItem look (unkeyed, so any future plain
         <TabControl> in the app is dark-themed automatically instead of hitting the same
         "unstyled -> OS-light chrome" bug every other control above already had) -
         vertical, flat, narrow tabs matching the Properties panel's own (keyed)
         PropertiesTabControlStyle/PropertiesTabItemStyle in MainWindow.xaml, which stay
         as they are (their own fixed Width="42" is specific to that one narrow strip) -
         this is the general-purpose fallback, not a replacement for those. Unlike a plain
         Setter-only Style, TabControl's own ControlTemplate is fully replaced here too
         (not just Background/BorderThickness/Padding Setters) - the DEFAULT WPF
         TabControl template is the one remaining place in this app that still drew the
         OS theme's own chrome around the tab strip/content area regardless of this
         Style's Setters, the exact "OS-default chrome, not this app's own dark Style"
         bug already fixed for every other non-trivial control (Menu, ComboBox, TreeView,
         ...) - left unfixed here, it's what produced the stray light/white divider and
         the washed-out header look. Assumes TabStripPlacement="Left" (this app's only
         actual usage), so the header strip is hardcoded to the left column rather than
         switching layout per TabStripPlacement value. -->
    <Style TargetType="{x:Type TabControl}">
        <Setter Property="Background" Value="{StaticResource PanelBackgroundBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabControl}">
                    <Grid ClipToBounds="True" KeyboardNavigation.TabNavigation="Local">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TabPanel x:Name="HeaderPanel" Grid.Column="0" Panel.ZIndex="1"
                                  IsItemsHost="True" Background="Transparent"
                                  KeyboardNavigation.TabIndex="1" />
                        <Border Grid.Column="1" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter x:Name="PART_SelectedContentHost"
                                              Margin="{TemplateBinding Padding}"
                                              ContentSource="SelectedContent" />
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type TabItem}">
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabItem}">
                    <Border x:Name="Bg" Padding="12,7"
                            Background="{StaticResource PanelAltBackgroundBrush}"
                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                        <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"
                                          TextBlock.FontSize="11" TextBlock.TextAlignment="Center"
                                          TextBlock.Foreground="{TemplateBinding Foreground}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="{StaticResource HoverBackgroundBrush}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="{StaticResource AccentBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- A compact "Label: [value]" row for the Properties Inspector's numeric
         Position/Rotation/Scale fields - a fixed-width label so all nine of them
         (three axes x three groups) line up in one column regardless of Position vs.
         Rotation vs. Scale's differing label widths. -->
    <Style x:Key="InspectorFieldLabelStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
        <Setter Property="Width" Value="16" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontSize" Value="11" />
    </Style>

    <Style x:Key="InspectorFieldTextBoxStyle" TargetType="{x:Type TextBox}" BasedOn="{StaticResource {x:Type TextBox}}">
        <Setter Property="Width" Value="64" />
        <Setter Property="Margin" Value="4,2" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>
</ResourceDictionary>
