<Window x:Class="AsmodayCat.UI.Views.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="#CC0D0D1A"
        Title="" Height="320" Width="620"
        WindowStyle="None"
        AllowsTransparency="True"
        Topmost="True"
        WindowStartupLocation="CenterScreen"
        Deactivated="Window_Deactivated">
    <Border CornerRadius="8" Background="#EE111120"
            BorderBrush="#B39DDB" BorderThickness="1">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="ASMODAYCAT" FontSize="12" FontWeight="Bold"
                       Foreground="#B39DDB" Margin="0,0,0,8"/>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"
                          Margin="0,0,0,8">
                <TextBlock x:Name="ResponseBlock"
                           TextWrapping="Wrap"
                           FontFamily="Consolas" FontSize="13"
                           Foreground="White"/>
            </ScrollViewer>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="QuickInput"
                         materialDesign:HintAssist.Hint="Quick query... (Esc to close)"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         KeyDown="QuickInput_KeyDown"/>
                <Button Grid.Column="1" Content="ASK"
                        Click="SendButton_Click"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Margin="8,0,0,0"/>
            </Grid>
        </Grid>
    </Border>
</Window>
