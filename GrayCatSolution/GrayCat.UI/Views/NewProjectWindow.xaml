<Window x:Class="GrayCat.UI.Views.NewProjectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:GrayCat.UI.ViewModels"
        Title="Create New Project" Height="400" Width="550"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Window.DataContext>
        <vm:NewProjectViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Project Name -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Project Name" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding ProjectName, UpdateSourceTrigger=PropertyChanged}" 
                     FontSize="14" Padding="8"/>
        </StackPanel>

        <!-- Author -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Author" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Author, UpdateSourceTrigger=PropertyChanged}" 
                     FontSize="14" Padding="8"/>
        </StackPanel>

        <!-- Output Path -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Location" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding OutputPath}" 
                         IsReadOnly="True" FontSize="14" Padding="8"/>
                <Button Grid.Column="1" Content="Browse..." Click="Browse_Click" 
                        Width="100" Margin="10,0,0,0" Padding="8"/>
            </Grid>
        </StackPanel>

        <!-- Project Type -->
        <StackPanel Grid.Row="3" Margin="0,0,0,15">
            <TextBlock Text="Project Type" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <ComboBox ItemsSource="{Binding ProjectTypes}" 
                      SelectedItem="{Binding SelectedProjectType}"
                      FontSize="14" Padding="8"/>
        </StackPanel>

        <!-- Progress -->
        <ProgressBar x:Name="ProgressBar" Grid.Row="4" Height="4" 
                     IsIndeterminate="True" Visibility="Collapsed"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Create" Width="100" IsDefault="True" 
                    Click="Create_Click" Margin="0,0,10,0" Padding="8"/>
            <Button Content="Cancel" Width="100" IsCancel="True" Padding="8"/>
        </StackPanel>
    </Grid>
</Window>